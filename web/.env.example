# Copy to .env.local for local development. The client id is public -- it is built into the
# page and is not a secret. The client SECRET is never here: it lives only in the Cloudflare
# Pages project environment, where the /api/oauth function reads it.
VITE_GITHUB_CLIENT_ID=
